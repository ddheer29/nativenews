import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const NewsSpecificScreen = () => {
  return (
    <View>
      <Text>NewsSpecificScreen</Text>
    </View>
  );
};

export default NewsSpecificScreen;

const styles = StyleSheet.create({});
